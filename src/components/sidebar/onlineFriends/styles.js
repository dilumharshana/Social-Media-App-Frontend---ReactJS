import { makeStyles } from "@material-ui/core";

export const styles = () =>
  makeStyles({
    names: {
      fontFamily: "arial",
      margin: "10px",
    },
    friendList: {
      listStyle: "none",
    },
  });
