import { makeStyles } from "@material-ui/core";

export const styles = () => {
  return makeStyles({
    shareContainer: {
      width: "100%",
      borderRadius: "10px",
      webkitBoxShadow: "0px 0px 20px -12px rgba(0,0,0,0.99)",
      boxShadow: "0px 0px 20px -12px rgba(0,0,0,0.99)",
    },
  });
};
